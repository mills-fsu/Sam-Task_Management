<Page
    x:Class="UWPListManagement.Dialogs.AttendeesDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWPListManagement.Dialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>

        <Grid.Resources>
            <SolidColorBrush x:Key="button" Color="#00a8f3"/>
            <SolidColorBrush x:Key="mouseOverButton" Color="#FF48F1F5"/>
            <SolidColorBrush x:Key="backgroundBorder" Color="#FF0211F3"/>

            <Style x:Key="buttonStyle" TargetType="Button">
                <Setter Property="BorderBrush" Value="{StaticResource backgroundBorder}"/>
                <Setter Property="Background" Value="{StaticResource mouseOverButton}"/>
                <Setter Property="BorderThickness" Value="1"/>
            </Style>
        </Grid.Resources>

        <Grid.RowDefinitions>
            <RowDefinition Height="2*"></RowDefinition>
            <RowDefinition Height="12*"></RowDefinition>

        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"></ColumnDefinition>
                <ColumnDefinition Width="1*"></ColumnDefinition>
                <ColumnDefinition Width="1*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Button x:ConnectionId='2' Grid.Column="0" Content="Save and Return" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"                        Style="{StaticResource buttonStyle}"></Button>
            <Button x:ConnectionId='3' Grid.Column="1" Content="Delete" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"                        Style="{StaticResource buttonStyle}"></Button>
            <Button x:ConnectionId='4' Grid.Column="2" Content="Add" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"                     Style="{StaticResource buttonStyle}"></Button>
        </Grid>
        <Grid Grid.Row="1">
            <ListBox ItemsSource="{Binding Path=Attendees, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" SelectedItem="{Binding Path=SelectedItem, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" ></ListBox>
        </Grid>
        
    </Grid>
</Page>

